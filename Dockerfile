# Используем официальный образ Python 3 в качестве базового
FROM python:3

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем текущие файлы проекта в рабочую директорию контейнера
COPY . /app

# Если у вас есть файл зависимостей requirements.txt, установите их (опционально)
# RUN pip install --no-cache-dir -r requirements.txt

# Команда для запуска Python-скрипта при старте контейнера
CMD ["python", "app.py"]
